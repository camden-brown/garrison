// Package arch holds Garrison's architecture tests.
//
// It has no production code. Its job is to make the dependency rule in
// docs/DESIGN.md fail the build rather than decay into a comment nobody reads.
package arch
